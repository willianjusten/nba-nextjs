/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // THIS IS JUST FOR NOW! REMOVE THIS WHEN YOU HAVE TYPES FOR EVERYTHING
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
